import logo from "../../../logo.svg";

import './LoadScreenComponent.css';

const LoadScreenComponent = ({}) => {
  return(
    <div className="">
      <p>cargando...</p>
      {/*<img className="load-logo"  src={logo} alt="chatbot"/>*/}
    </div>
  );
};

export default LoadScreenComponent;
